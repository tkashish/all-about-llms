[project]
name = "all-about-llms"
version = "0.1.0"
description = "A transformer language model and BPE tokenizer, built from scratch."
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.4",
    "regex>=2026.4.4",
    "torch>=2.11.0",
]

[tool.uv]
package = false
